language: node_js
node_js:
  - "node"
  - "10"
dist: xenial
cache:
  npm: true
matrix:
  fast_finish: true
services:
  - mongodb
install:
  - npm install --prefix api
before_script:
  - sleep 15
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'
script:
  - npm test --prefix api